import { SpinnerContainer, LoadingSpinner } from "./Spinner.style";

const Spinner = () => {
  return (
    <SpinnerContainer>
      <LoadingSpinner />
    </SpinnerContainer>
  );
};

export default Spinner;
