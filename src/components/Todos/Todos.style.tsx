import styled from "styled-components";
import { Container } from "../CreateTodo/CreateTodo.style";

export const TodosContainer = styled(Container)`
  flex-direction: column;
  padding: 0;
  margin-top: 1rem;
`;
